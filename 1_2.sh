#2. Shell-программа периодически с некоторым интервалом удаляет все временные файлы с указанным суффиксом (например, .tmp)
#в поддереве, начиная с каталога, имя которого задано параметром Shell-программы и ведет лог действий формата:
#дата/время     : удалённые файлы
#15.02.09T21:00 : file1 file2 file3



#!/bin/bash

# Проверка количества аргументов
if [ $# -ne 3 ]; then
    echo "Ошибка: неверное количество аргументов(!=3)" >&2
    exit 1
fi

TARGET_DIR="$1"
SUFFIX="$2"
INTERVAL="$3"
# Лог-файл в текущей директории (откуда запускается скрипт)
LOG_FILE="./tmp_cleaner.log"

# Проверка существования директории
if [ ! -d "$TARGET_DIR" ]; then
    echo "Ошибка: директория '$TARGET_DIR' не существует" >&2
    exit 2
fi

# Проверка что интервал - число
if ! [[ "$INTERVAL" =~ ^[0-9]+$ ]] || [ "$INTERVAL" -le 0 ]; then
    echo "Ошибка: интервал должен быть положительным числом (в секундах)" >&2
    exit 3
fi

# Проверка/создание лог-файла в текущей директории
if [ ! -f "$LOG_FILE" ]; then
    touch "$LOG_FILE"
    echo "Создан лог-файл в текущей директории: $LOG_FILE" >&2
fi

echo "Запуск очистки временных файлов с суффиксом '$SUFFIX'"
echo "Директория: $TARGET_DIR"
echo "Интервал: $INTERVAL секунд"
echo "Лог-файл: $LOG_FILE"
echo "Для остановки нажмите Ctrl+C"
echo "------------------------------------------------------"

# Основной цикл очистки
while true; do
    # Получаем текущую дату и время в нужном формате
    TIMESTAMP=$(date +"%y.%m.%dT%H:%M")
    
    # Ищем и удаляем файлы, собираем список удаленных
    # Переходим в целевую директорию, чтобы работать с относительными путями
    cd "$TARGET_DIR"
    DELETED_FILES=$(find . -type f -name "*$SUFFIX" 2>/dev/null)
    
    if [ -n "$DELETED_FILES" ]; then
        # Удаляем файлы и получаем список удаленных
        COUNT=0
        DELETED_LIST=""
        
        for file in $DELETED_FILES; do
            if rm -f "$file" 2>/dev/null; then
                # Убираем начальную ./ если есть
                clean_file="${file#./}"
                DELETED_LIST="$DELETED_LIST $clean_file"
                COUNT=$((COUNT + 1))
            fi
        done
        
        # Возвращаемся в исходную директорию
        cd - > /dev/null
        
        # Записываем в лог
        echo "$TIMESTAMP :$DELETED_LIST" >> "$LOG_FILE"
        
        # Выводим информацию на экран
        echo "[$TIMESTAMP] Удалено $COUNT файлов с суффиксом '$SUFFIX'"
    else
        cd - > /dev/null
        echo "[$TIMESTAMP] Файлы с суффиксом '$SUFFIX' не найдены"
    fi
    
    # Ждем указанный интервал
    sleep "$INTERVAL"
done





НАЧАЛО
  ЕСЛИ количество аргументов ≠ 3 ТО
    вывести ошибку и выйти
  КОНЕЦ ЕСЛИ
  
  сохранить аргументы: директория, суффикс, интервал
  
  ЕСЛИ директория не существует ТО
    вывести ошибку и выйти
  КОНЕЦ ЕСЛИ
  
  ЕСЛИ интервал не положительное число ТО
    вывести ошибку и выйти
  КОНЕЦ ЕСЛИ
  
  создать лог-файл если его нет
  
  ВЫВЕСТИ информацию о запуске
  
  ПОКА истина ЦИКЛ
    получить текущую дату/время в формате ГГ.ММ.ДДTЧЧ:ММ
    
    перейти в целевую директорию
    найти все файлы с указанным суффиксом
    
    ЕСЛИ файлы найдены ТО
      ДЛЯ каждого файла ЦИКЛ
        удалить файл
        добавить имя файла в список удаленных
        увеличить счетчик
      КОНЕЦ ЦИКЛА
      
      вернуться в исходную директорию
      записать в лог: время + список файлов
      вывести статистику
    ИНАЧЕ
      вывести сообщение что файлов нет
    КОНЕЦ ЕСЛИ
    
    ждать заданный интервал
  КОНЕЦ ЦИКЛА
КОНЕЦ
